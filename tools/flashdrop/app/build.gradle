plugins {
    id 'com.android.application'
}

android {
    namespace 'in.flashdrop.lan'
    compileSdk 35

    defaultConfig {
        applicationId 'in.flashdrop.lan'
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
